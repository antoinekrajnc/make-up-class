<h1 class="calendar-title d-flex justify-content-center mt-4 mb-3">Week Calendar</h1>
<h6 class="intro-p mb-5">If you missed a class or will miss a class, you can make it up! Below you will see the week calendar of our classes. Click on "Makeup" to see if a spot is available and book it. </h6>

</div>
<div class="container d-flex flex-row center justify-content-around">
  <% for i in 0..6 do %>
  <% date = Date.today + i %>
  <% weekday = date.wday %>
    <% if weekday == 1 %>
      <% weekday = "Monday" %>
      <% elsif weekday == 2 %>
      <% weekday = "Tuesday" %>
      <% elsif weekday == 3 %>
      <% weekday = "Wednesday" %>
      <% elsif weekday == 4 %>
      <% weekday = "Thursday" %>
      <% elsif weekday == 6 %>
      <% weekday = "Saturday" %>
    <%end%>
    <% if weekday == 0 || weekday == 5 %>
    <% else %>
     <div class = "class-day rounded col-2">
       <h6 class="d-flex justify-content-center"><%="#{weekday}"%></h6>
        <h6 class="d-flex justify-content-center mb-4"><%= "#{date.month}-#{date.day}"%></h6>
        <hr class="separator m-0 ml-2"></hr>
          <% for j in 1..Cohort.all.size do %>
            <% cohort = Cohort.find(j) %>
              <% cohort.attributes.each_pair do |name, value| %>
                <% if date == value %>
                <div class="d-flex flex-row align-items-center">
                    <div = "d-flex flex-start">
                      <p class="row-1 m-0 ml-2 mt-4">Cohort: <%="#{cohort.title[0..7]}"%></p>
                      <p class="row-1 m-0 ml-2">Class: <%= "#{name[5..7]}"%></p>
                    </div>
                    <button class="makeup-button mt-3 ml-2"><%=link_to "Makeup", new_makeup_path(@makeup, clicked_cohort:"#{cohort.title}", clicked_day: "#{name}")%></button>
                </div>
                <% end %>
              <% end %>
          <%end%>
       </div>
      <% end %>
  <% end %>
</div>

 <div class="more-dates">
   <button class="more-dates-button mt-3 ml-2"><%=link_to "More Dates", all_dates_path()%></button>
 </div>
